{% extends 'base.html.twig' %}

{% block title %}Modifier Membre{% endblock %}

{% block body %}
	<main class="container-fluid">
		<article class="container">
			<h1 class="text-center mb-4 fw-semibold fs-2">Modifier Membre</h1>
			{{ include('membre/_form.html.twig', {'button_label': 'Update'}) }}
			<a href="{{ path('membre_index') }}" class="btn btn-outline-primary btn-sm float-end ms-3">Retour</a>
			{{ include('membre/_delete_form.html.twig') }}
		</article>
	</main>
{% endblock %}
