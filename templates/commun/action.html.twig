<div class="btn-group">
	<a href="{{ path(show, {'id': id}) }}" class="btn btn-sm btn-outline-secondary">
		<i class="bi bi-search"></i>
	</a>
	<a href="{{ path(edit, {'id': id}) }}" class="btn btn-sm btn-outline-secondary">
		<i class="bi bi-pencil-square"></i>
	</a>
	<a href="{{ path(delete, {'id': id}) }}" class="btn btn-sm btn-outline-secondary">
		<i class="bi bi-trash3-fill"></i>
	</a>
</div>
