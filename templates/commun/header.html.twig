<header class="sticky-top bg-danger mb-5">
	<nav class="navbar navbar-expand navbar-dark container px-3">
		<a class="navbar-brand" href="{{ path('home') }}">MyShop</a>
		<ul class="navbar-nav ms-auto">
			<li class="nav-item">
				<a class="nav-link" href="{{ path('home') }}">Accueil</a>
			</li>
			{% if is_granted('ROLE_MEMBRE') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('panier') }}">Panier</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('commande_user_passe') }}">Envoyé</a>
				</li>
			{% endif %}
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="dropGestion" role="button" data-bs-toggle="dropdown" aria-expanded="false">Gestion</a>
					<ul class="dropdown-menu" aria-labelledby="dropGestion">
						<li>
							<a class="dropdown-item" href="{{ path('commande_index') }}">Liste Commandes</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('commande_new') }}">Ajouter Commande</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{ path('membre_index') }}">Liste Membres</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('membre_new') }}">Ajouter Membre</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{ path('produit_index') }}">Liste Produits</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('produit_new') }}">Ajouter Produit</a>
						</li>
					</ul>
				</li>
			{% endif %}
			{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('login') }}">Connexion</a>
				</li>
			{% endif %}
		</ul>
	</nav>
</header>
